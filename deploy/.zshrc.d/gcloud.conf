# Try user-local install first, fall back to system install
if [ -d "$HOME/.local/src/google-cloud-sdk" ]; then
    export CLOUDSDK_ROOT_DIR="$HOME/.local/src/google-cloud-sdk"
elif [ -d "/usr/share/google-cloud-sdk" ]; then
    export CLOUDSDK_ROOT_DIR="/usr/share/google-cloud-sdk"
fi

# The next line updates PATH for the Google Cloud SDK.
if [ -f "$CLOUDSDK_ROOT_DIR/path.zsh.inc" ]; then . "$CLOUDSDK_ROOT_DIR/path.zsh.inc"; fi

# The next line enables shell command completion for gcloud.
if [ -f "$CLOUDSDK_ROOT_DIR/completion.zsh.inc" ]; then . "$CLOUDSDK_ROOT_DIR/completion.zsh.inc"; fi

